/**
 * The recipe overlay. One public entry point: it reads the selected drink and the open flag off
 * `lab.context`, so the screen mounts it and there is nothing to thread.
 */
export { RecipeOverlay } from './recipe.overlay'
