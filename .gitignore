node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# host CLI state — `netlify link` writes .netlify/state.json on first setup
.netlify

# render pipeline scratch
.render-work

# font subsetting venv
.fontenv
